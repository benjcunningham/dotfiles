name: minimal
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
    steps:
    - name: Installation
      run: |
        curl -fsSLO https://raw.githubusercontent.com/benjcunningham/dotfiles/master/install.sh
        bash install.sh -m
