name: full
on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
        - macos-latest
        - ubuntu-latest
    steps:
    - name: Installation
      run: |
        curl -fsSLO https://raw.githubusercontent.com/benjcunningham/dotfiles/master/install.sh
        bash install.sh -pw
